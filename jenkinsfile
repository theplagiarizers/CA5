pipeline {
    agent any
    
    stages {
  
        stage('Run Services') {
            steps {
                // Deploy your application using Docker Compose
                sh 'docker-compose up -d'
            }
        }
    }
}