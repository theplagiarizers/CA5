pipeline {
    agent any
    
    stages {
  
        stage('Run Services') {
            steps {

                sh 'docker compose version'
                // Deploy your application using Docker Compose
                sh 'docker-compose up -d'
            }
        }
    }
}